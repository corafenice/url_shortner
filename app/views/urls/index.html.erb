<p id="notice"><%= notice %></p>
<div class="main">
  <h1>Short Urls</h1>
  <h2>This is a URL shortner.
    <br/>
    <br/>
    It works!
    <br/>
    All you have to do it type the long url.
    <br/>

  </h2>

  <table>
    <thead>
      <tr>
        <th>Long URL</th>
        <th>Shortened Url</th>
        <th>ID</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody class="long">
      <% @urls.each do |url| %>
      <tr>
        <td><%= url.long %></td>
        <td><%= url.short %></td>
        <td><%= url.id %></td>
        <td><%= link_to 'Show', @url %></td>
        <td><%= link_to 'Edit', edit_url_path(url) %></td> <td><%= link_to 'Destroy', url, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'Click HERE to get a URL shortened!', new_url_path %>
</div>
